{
  "name": "budget-control-server",
  "version": "1.0.0",
  "description": "เซิร์ฟเวอร์สำหรับแอปพลิเคชันควบคุมงบประมาณ",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-client": "cd client && npm run build",
    "deploy": "npm run build-client && npm start"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "google-auth-library": "^9.6.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}